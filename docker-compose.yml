services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: family_grocery
      POSTGRES_USER: grocery
      POSTGRES_PASSWORD: grocery
    ports:
      - "5432:5432"
    volumes:
      - grocery-postgres:/var/lib/postgresql/data

volumes:
  grocery-postgres:
